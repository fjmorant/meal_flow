# Firebase config is handled via native files, not env vars.
# Download these from Firebase Console → Project Settings:
#   - google-services.json       → place in project root (Android)
#   - GoogleService-Info.plist   → place in project root (iOS)
#
# Then run: pnpm prebuild
